# server/Dockerfile.metrics
FROM node:20-alpine

WORKDIR /app

# Copy the metrics server script
COPY metrics.js .

# Install dependencies
RUN npm install prom-client express

# Expose metrics port
EXPOSE 8082

# Run the metrics server
CMD ["node", "metrics.js"]
