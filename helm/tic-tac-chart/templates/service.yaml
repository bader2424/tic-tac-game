apiVersion: v1
kind: Service
metadata:
  name: {{ include "tic-tac-chart.fullname" . }}
  labels:
    {{- include "tic-tac-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    {{- include "tic-tac-chart.selectorLabels" . | nindent 4 }}
