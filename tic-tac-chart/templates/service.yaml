apiVersion: v1
kind: Service
metadata:
  name: {{ include "tic-tac-chart.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: {{ include "tic-tac-chart.name" . }}
